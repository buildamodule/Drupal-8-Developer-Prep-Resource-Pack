* Create a folder called /src/Calendar
* Create a folder called /src/Calendar/Controller and move LeapYearController.php into it
* Create a folder called /src/Calendar/Model and move LeapYear.php into it